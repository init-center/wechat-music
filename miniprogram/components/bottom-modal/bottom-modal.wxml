<view class="modal" hidden="{{ !isModalShow }}">
  <view class="panel">
    <i class="iconfont icon-delete" bind:tap="onHideModal"></i>

    <!-- 使用插槽 -->
    <slot name="modal-content"></slot>
  </view>
</view>